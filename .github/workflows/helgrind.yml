name: Helgrind

# Thread-error (data-race / lock-order) detection under Valgrind Helgrind.
# nginx workers are separate processes, but the module touches shared-memory
# zones + (with --with-threads) the aio thread pool — helgrind covers that.
# WIDE+LONG monthly run on the self-hosted builder02 lxc runner; replaces the
# local nginx-module-stress cron's coverage. Mirrors valgrind.yml, tool swapped.
#
# Action pins (see build-test.yml for rationale):
# actions/checkout@v5        -> 93cb6efe18208431cddfb8368fd83d5badbf9bfd
# actions/upload-artifact@v5 -> 330a01c490aca151604b8cf639adc76d48f6c5d4

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  NGINX_VERSION: "1.31.1"

on:
  schedule:
    # Monthly, 1st @ 04:30 UTC — offset from fuzz (03:41) and valgrind (03:30).
    - cron: "30 4 1 * *"
  workflow_dispatch: {}

concurrency:
  group: helgrind-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  helgrind:
    name: Helgrind
    runs-on: [self-hosted, builder02, lxc]
    timeout-minutes: 240
    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential curl libhiredis-dev libpcre2-dev redis-server \
            valgrind zlib1g-dev

      - name: Cache source tarball
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: .build/nginx-${{ env.NGINX_VERSION }}.tar.gz
          key: src-nginx-${{ env.NGINX_VERSION }}

      - name: Build debug server and module
        run: bash tools/ci-build.sh nginx "$NGINX_VERSION" debug

      - name: Run runtime suite under Helgrind
        run: |
          runner="valgrind --tool=helgrind --error-exitcode=99"
          runner="$runner --suppressions=$GITHUB_WORKSPACE/tools/valgrind.supp"
          runner="$runner --log-file=$GITHUB_WORKSPACE/helgrind-%p.log"
          python3 tools/test_runtime.py \
            --single-process \
            --runner "$runner" \
            --redis-server /usr/bin/redis-server \
            --nginx-binary "$GITHUB_WORKSPACE/.build/nginx-${NGINX_VERSION}/objs/nginx" \
            --module "$GITHUB_WORKSPACE/.build/nginx-${NGINX_VERSION}/objs/ngx_http_error_abuse_module.so"

      - name: Verify Helgrind reports
        if: always()
        run: |
          cat helgrind-*.log || true
          if grep -Eq 'ERROR SUMMARY: [1-9][0-9]* errors' helgrind-*.log; then
            exit 1
          fi
          grep -Eq 'ERROR SUMMARY: 0 errors' helgrind-*.log

      - name: Upload Helgrind reports
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: helgrind-reports
          path: helgrind-*.log
          retention-days: 14

      - name: Notify Discord on failure
        if: failure() && github.event_name == 'schedule'
        env:
          DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
          DISCORD_CHANNEL_BUILDS: ${{ secrets.DISCORD_CHANNEL_BUILDS }}
        run: |
          [ -n "$DISCORD_BOT_TOKEN" ] || exit 0
          curl -fsS -X POST \
            -H "Authorization: Bot $DISCORD_BOT_TOKEN" \
            -H "Content-Type: application/json" \
            "https://discord.com/api/v10/channels/$DISCORD_CHANNEL_BUILDS/messages" \
            -d "{\"content\":\"🔴 ${{ github.repository }} — ${{ github.workflow }} FAILED (scheduled). ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\"}" || true
